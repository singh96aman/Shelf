<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Shopping">
        <menu>
        <item
            android:id="@+id/home"
            android:icon="@drawable/ic_action_name"
            android:title="Home"
            />
        <item
            android:id="@+id/offers"
            android:icon="@drawable/ic_action_offers"
            android:title="Offers" />
        <item
            android:id="@+id/gifts"
            android:icon="@drawable/ic_stat_gift"
            android:title="Gifts" />
        </menu>
    </item>

    <item android:title="Account">
        <menu>
            <item
                android:id="@+id/login"
                android:icon="@drawable/ic_login"
                android:title="Login" />
            <item
                android:id="@+id/cart"
                android:icon="@drawable/ic_kart"
                android:title="Cart" />
        </menu>
    </item>

</menu>
